<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>
    <%= title %>
  </title>
  <script type="text/javascript" src="https://ajax.googleapis.com/ajax/libs/angularjs/1.5.6/angular.min.js"></script>
  <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/1.7.3/socket.io.js"></script>
  <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/angular-socket-io/0.7.0/socket.js"></script>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <script src="app.js"></script>

</head>

<body ng-app='rt-arduino' ng-controller='report-controller'>
  <div class="container">
    <div class="row" style="padding-top: 70px">
      <div class="col-lg-4 col-sm-12">
        <div class="row">
          <h1 style="text-align:center"> Socket Example </h1>
          <h3 style="text-align:center"> firmware: {{firmware}} </h3>
        </div>
        <div class="row">

          <table class="table table-striped">
            <thead>
              <tr>
                <th>Pin</th>
                <th>Value</th>
                <th>time</th>
              </tr>
            </thead>
            <tbody>
              <tr ng-repeat="change in changes">
                <td>{{change.pin}}</td>
                <td>{{change.value}}</td>
                <td>{{change.time | date : "medium" }}</td>
              </tr>
            </tbody>
          </table>

        </div>

      </div>
    </div>
  </div>
</body>

</html>